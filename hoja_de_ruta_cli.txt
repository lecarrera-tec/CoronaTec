* Selección única, sin parametrizar, banco de preguntas
    [Generar un sólo string de texto para la pregunta en latex.] Listo!
  - Incluir la posibilidad de que se escojan varios archivos por carpeta.
  - Definir cómo hacer para una nueva página. Tal vez utilizar 
    \begin{samepage} ... \end{samepage} y \nopagebreak. El primer evita que se
    salte de página en medio de un párrafo. El segundo evita saltos de
    página entre párrafos.
  - Leer carpeta con las listas de los grupos para generar pdfs. Los
    argumentos deberían de ser el archivo .ppp y la carpeta con la lista de
    los grupos de estudiantes. Archivos CVS. Se les quita la extensión, y se
    genera una carpeta por grupo en el lugar de trabajo.
  - Resolver el problema de incluir figuras de la carpeta donde está el
    programa. Una posibilidad es utilizar la función del paquete *graphicx*, 
    el problema que tiene es que habría que incluir cada una de las carpetas,
    y podría pasar que haya dos figuras con el mismo nombre en carpetas
    distintas.
  - Programar la revisión. Los argumentos deberían de ser el archivo .ppp,
    la carpeta con las listas de los estudiantes, y el archivo CVS generado
    por google forms. Generar una interfaz. Se podría utilizar inicialmente
    diccionarios, donde la llave sea el número de carnet del estudiante.
  - Probar con un cuiz real. Incluir la opción de que haya que anular una
    pregunta mediante dos opciones: *cualquiera*, se le dan los puntos, 
    y *ninguna*, se elimina la pregunta (equivalente a poner 0 puntos en el
    valor de la pregunta).

* Respuesta corta, sin parámetros, banco de preguntas.
  - respuesta entera;
  - respuesta con punto flotante. Incluir error relativo, error nominal.

* Respuesta corta, preguntas parametrizadas.
  - Generar diccionario para variables enteras.
  - Definir e implementar @|   | para incluir los valores de los parámetros
    en las preguntas y los ítemes.
  - Implementar algunas funciones. Lo importante es la estructura para incluir
    funciones nuevas.
  - Generar diccionario para variables de punto flotante. Definir el número
    de dígitos que se van a utilizar para mostrar valores.

* Selección única, parametrizadas.
  - Items en orden fijo
  - Items con índice dado (parametrizado) 
  - Items en orden creciente.
